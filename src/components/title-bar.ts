import colors from '@/constants/colors';
import { WorkbenchColors } from '@/types';

const titleBar: WorkbenchColors = {
  'titleBar.activeBackground': colors.base.background,
  'titleBar.inactiveBackground': colors.base.background,
  'titleBar.activeForeground': colors.base.foreground,
  'titleBar.inactiveForeground': colors.base.foreground,
  'menu.selectionBackground': colors.accent.base,
};

export default titleBar;
