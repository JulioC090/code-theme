import colors from '@/constants/colors';
import { WorkbenchColors } from '@/types';

const general: WorkbenchColors = {
  foreground: colors.base.foreground,
  focusBorder: colors.primary.base,
  'progressBar.background': colors.primary.base,
  'selection.background': colors.accent.base,
};

export default general;
