import colors from '@/constants/colors';
import { WorkbenchColors } from '@/types';

const dropdown: WorkbenchColors = {
  'dropdown.background': colors.widget.background,
  'dropdown.foreground': colors.widget.foreground,
  'dropdown.listBackground': colors.widget.background,
};

export default dropdown;
