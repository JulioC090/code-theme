import colors from '@/constants/colors';
import { WorkbenchColors } from '@/types';

const activityBar: WorkbenchColors = {
  'activityBar.background': colors.base.background,
  'activityBar.foreground': colors.primary.base,
  'activityBarBadge.background': colors.accent.base,
  'activityBarBadge.foreground': colors.accent.foreground,
  'activityBar.border': colors.base.border,
};

export default activityBar;
