import colors from '@/constants/colors';
import { WorkbenchColors } from '@/types';

const tabs: WorkbenchColors = {
  'editorGroupHeader.tabsBackground': colors.base.background,
  'tab.inactiveBackground': colors.base.background,
  'tab.activeBorderTop': colors.primary.base,
  'tab.border': colors.base.border,
};

export default tabs;
