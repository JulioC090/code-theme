import colors from '@/constants/colors';
import { WorkbenchColors } from '@/types';

const buttons: WorkbenchColors = {
  'button.background': colors.primary.base,
  'textLink.foreground': colors.primary.base,
  'textLink.activeForeground': colors.primary.hover,
};

export default buttons;
