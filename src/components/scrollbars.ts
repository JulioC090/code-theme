import colors from '@/constants/colors';
import { WorkbenchColors } from '@/types';

const scrollbars: WorkbenchColors = {
  'scrollbarSlider.background': colors.input.background,
};

export default scrollbars;
