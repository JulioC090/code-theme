import colors from '@/constants/colors';
import { WorkbenchColors } from '@/types';

const widgets: WorkbenchColors = {
  'pickerGroup.foreground': colors.primary.base,
  'listFilterWidget.background': colors.widget.background,
  'listFilterWidget.noMatchesOutline': colors.primary.base,
  'editorWidget.background': colors.widget.background,
  'editorWidget.foreground': colors.widget.foreground,
  'editorWidget.resizeBorder': colors.accent.base,
  'debugToolBar.background': colors.widget.background,
};

export default widgets;
