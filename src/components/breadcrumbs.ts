import colors from '@/constants/colors';
import { WorkbenchColors } from '@/types';

const breadcrumbs: WorkbenchColors = {
  'breadcrumb.background': colors.base.background,
  'breadcrumb.foreground': colors.accent.base,
  'breadcrumb.focusForeground': colors.primary.base,
};

export default breadcrumbs;
