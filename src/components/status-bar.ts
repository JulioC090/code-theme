import colors from '@/constants/colors';
import { WorkbenchColors } from '@/types';

const statusBar: WorkbenchColors = {
  'statusBar.background': colors.accent.base,
  'statusBar.debuggingBackground': colors.accent.base,
  'statusBar.noFolderBackground': colors.accent.base,
  'statusBarItem.remoteBackground': colors.accent.base,
};

export default statusBar;
